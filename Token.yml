info:
  name: Token
  type: http
  seq: 2

http:
  method: GET
  url: https://genterrh.kommo.com/oauth2/access_token
  headers:
    - name: User-Agent
      value: insomnia/12.6.0

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
